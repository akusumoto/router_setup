{
  "annotations": { "list": [] },
  "editable": true,
  "panels": [
    {
      "datasource": { "type": "prometheus", "uid": "" },
      "fieldConfig": { "defaults": { "unit": "short" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 0 },
      "id": 1,
      "targets": [{ "expr": "node_load1", "refId": "A" }],
      "title": "Load (1 minute)",
      "type": "stat"
    },
    {
      "datasource": { "type": "prometheus", "uid": "" },
      "fieldConfig": { "defaults": { "unit": "bytes" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 0 },
      "id": 2,
      "targets": [{ "expr": "node_memory_MemAvailable_bytes", "refId": "A" }],
      "title": "Available memory",
      "type": "stat"
    },
    {
      "datasource": { "type": "prometheus", "uid": "" },
      "fieldConfig": { "defaults": { "unit": "Bps" }, "overrides": [] },
      "gridPos": { "h": 10, "w": 24, "x": 0, "y": 8 },
      "id": 3,
      "targets": [
        { "expr": "rate(node_network_receive_bytes_total{device=~\"eth0|br-lan\"}[5m])", "legendFormat": "{{device}} RX", "refId": "A" },
        { "expr": "rate(node_network_transmit_bytes_total{device=~\"eth0|br-lan\"}[5m])", "legendFormat": "{{device}} TX", "refId": "B" }
      ],
      "title": "WAN and LAN traffic",
      "type": "timeseries"
    }
  ],
  "schemaVersion": 41,
  "tags": ["openwrt", "phase3c"],
  "title": "OpenWrt Overview",
  "uid": "openwrt-overview",
  "version": 1
}
